apiVersion: v1
kind: Service
metadata:
  name: {{ include "suna.fullname" . }}-api
  labels:
    {{- include "suna.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.backend.api.service.type }}
  ports:
    - port: {{ .Values.backend.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "suna.api.selectorLabels" . | nindent 4 }}
